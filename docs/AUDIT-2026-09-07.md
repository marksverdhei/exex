# exex repo audit — 2026-09-07

Scope: `main @ c5d906f`. Method: read every module and script, ran the 54-test
CPU suite, `ruff`/`vulture`/`pyright`, exercised every CLI end-to-end on a tiny
Gemma 4 MoE fixture (CPU), cross-checked against the 26B GH200 run (exex#10)
and a fresh cartridge round-trip at 26B (job 2175640).

## 1. Feature matrix

| Feature (as promised in README / issues / docstrings) | Status | Evidence |
|---|---|---|
| Config-driven arch descriptor (`arch.py`), no hardcoded 128/8 | **done** | 6 tests; used by every module; 26B + 129-expert clone work |
| View-based in-place expert training (`surgery.py`) | **done** | 9 tests; 26B run trains, saves cleanly after `finalize` |
| KL-anchored router co-training (`trainer.py`) | **done, with caveats** | 7 tests; works at 26B. Trains *all* router params → cartridge lossy (#26); Gemma4-specific internals (#32); no attention mask (#25) |
| `train_expert.py` CLI | **partial** | Runs; pad-token loss bug (#25), `--clone_from` arg wart (#30), no accumulation/schedule/eval/metrics/cartridge save (#31) |
| Grow / remove / label experts (`manager.py`, `manage_expert.py`) | **done** | 3 tests + CLI smoke; config `num_experts` syncs; 26B clone arm in flight |
| Cartridge format v0 (`cartridge.py`, `extract_experts.py`) | **done** | 6 tests; 26B: 120 tensors bit-exact vs checkpoint, 341 MB per expert |
| Merge: replace / blend / grow (`merger.py`, `merge_experts.py`) | **done** | 7 tests; CLI smoke incl. grow-to-5 and JSON batch path; fingerprint guard. fp16 load fixed in #23 |
| Pruning: utilisation / magnitude / "reap", remove / zero (`pruner.py`) | **done (reap mislabelled)** | 10 tests; CLI smoke both modes. "reap" = gate × *weight* norm, not REAP (#35) |
| Router stats / perplexity eval CLIs | **done** | Used for all 26B numbers; eager forced (#24) |
| Routing analyzer (`analyzer.py`, `run_analysis.py`) | **broken** | `output_router_logits` kwarg rejected by transformers 5 → crashes on load; 0 tests (#27) |
| `generate_report.py` | **mock-only** | Works with `--model_path mock` (self-contained fake model); real-model path hits #27; unrelated "nordic" mock data |
| Pluggable backends (`backends.py`, #6) | **stub** | Registry + torch backend; `unsloth` raises NotImplementedError by design |
| Memory-optimised training / VRAM bounds (#5) | **planned, not started** | Adam state only on views is true by construction; no checkpointing, no measured lower bound. 26B: 58 GB bs1 / 68 GB bs2 |
| 4-bit frozen base (`--load_in_4bit`) | **untested** | Code path exists; never exercised (bitsandbytes not in container) |
| CI (`.github/workflows-disabled/ci.yml`) | **planned-but-disabled** | Never ran; PRs #12–#22 merged without it (#29) |
| LICENSE (Apache-2.0 badge) | **missing** | No file (#28) |
| Console entry points / `pip install` CLI | **missing** | `sys.path.append` hack in every script (#34) |
| LoRA-per-expert (#11) | **abandoned (decided)** | Full-rank views chosen; README aligned |
| `examples/`, `requirements.txt`, `utils.py` (old README) | **abandoned** | Removed from README in #3 |

Summary: 8 done, 3 partial/caveated, 2 broken, 2 stub/planned, 3 missing, 2 deliberately abandoned.

## 2. Debug sweep

* **Test suite**: exists, 54 tests, all pass on CPU in ~40 s (`transformers 5.16.1`, `torch 2.14 cpu`). No coverage tool configured. No CLI or analyzer tests.
* **Static**: `ruff` 116 findings (85 whitespace; real: 7 unused imports, `zip` without `strict`, default-arg `cuda.is_available()`). `pyright` clean apart from env import resolution and one `Optional` narrowing in `pruner.score_experts` (guarded at runtime). `vulture`: `get_labels` unused, `has_shared_mlp`/`top_k_key` computed but unused.
* **CLI exercise (tiny fixture)**: train ✓, train+clone ✓ (config → 5 experts, reload ✓), eval ✓, extract ✓, merge@slot ✓, merge-grow ✓, prune reap/remove ✓, prune magnitude/zero ✓, manage add/remove ✓, router_stats ✓, run_analysis ✗ (#27), generate_report mock ✓ / real ✗.
* **Bugs confirmed**: pad tokens in loss (#25, 39 % of loss terms in the toy batch); cartridge round-trip not tensor-exact (#26, ΔPPL 0.004 toy / 0.0006 at 26B); analyzer dead (#27); `-0.000000` KL display.
* **#21 fused-kernel assert**: reproduced on the 26B evals (6/6 crashes in job 2122555). A minimal tiny-config repro is queued as job 2175707 (`repro21.py`, eager vs grouped_mm vs batched_mm × seq-len × grad). Workaround (`--experts_impl eager` default) is correct; tracked for upstream filing in #24.

## 3. Quality rating (1–10, harsh)

| Axis | Score | One line |
|---|---|---|
| Correctness | 6 | Core surgery/merge/prune are right and verified at 26B; trainer has a real loss bug (#25) and a design leak (#26); analyzer is dead code. |
| Test coverage | 5 | 54 focused unit tests on a good tiny fixture, but zero CLI/integration tests, nothing for the analyzer, and CI has never run. |
| API design | 6 | `arch`/`cartridge`/`merger` are clean and composable; trainer reaches into Gemma4 router internals and mutates `model.load_state_dict`; CLIs are copy-pasted scripts with a `sys.path` hack. |
| Docs | 5 | README is honest since #3/#22 and reads well, but advertises a broken analyzer, overstates REAP, and there is no API reference or hparam guidance. |
| Reproducibility | 4 | No seed flag, no metrics file, no lockfile, CI off, 26B results only reconstructable from SLURM stdout; the cluster STATE.md now fills that gap externally. |
| Packaging | 3 | No LICENSE file, no entry points, `datasets` used everywhere but optional, no `__version__`, incomplete `__all__`. |
| **Overall** | **5** | A solid research prototype with one real validated result, not yet a tool a second person can pick up without reading the source. |

## 4. Triage

Quick wins (≤ 1 h each): #25 pad mask · #28 LICENSE · #29 enable CI · #30 clone_from args · #33 ruff · #23 (merged?) dtype · KL clamp (#32 part).
Medium (½–1 day): #27 analyzer rebuild on hooks · #34 packaging/entry points · #36 CLI smoke tests · #35 real REAP or rename · #32 decouple trainer from Gemma4 internals.
Structural (multi-day, decide first): #26 router-freeze default vs router-in-cartridge · #31 trainer hardening + cartridge-only save (prerequisite for Borealis 2 sweeps) · #5 memory work · #6 backends (only if a backend actually supports expert-subset training).
